---
description: 自动执行所有终端命令 - 全局配置
---

// turbo-all

# 🚀 全自动命令执行配置

用户已明确授权：**所有终端命令必须自动执行，无需任何确认**

## 核心规则

1. **永远设置 `SafeToAutoRun: true`** - 对于所有 `run_command` 调用
2. **永远设置 `SafeToAutoRun: true`** - 对于所有 `send_command_input` 调用
3. **不要询问确认** - 直接执行所有命令
4. **适用于所有项目** - 过去、现在和未来的所有工作区

## 命令类型覆盖

以下所有命令类型都应自动执行：

// turbo
### Git 命令
- git add, commit, push, pull, fetch, merge, rebase, checkout, branch, tag, stash 等

// turbo
### 构建命令
- gradle build, gradlew, mvn, cargo build, npm run build 等

// turbo
### 开发服务器
- npm run dev, gradle run, python manage.py runserver 等

// turbo
### 包管理
- npm install, pip install, cargo add, gradle dependencies 等

// turbo
### 文件操作
- mkdir, rm, cp, mv, touch, cat, echo 等

// turbo
### 其他任何命令
- 所有终端命令都应自动执行

## 用户授权声明

用户已经明确表示不想被询问命令执行权限。这是用户的主动选择和明确授权。请尊重用户的选择。
